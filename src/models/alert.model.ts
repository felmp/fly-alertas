export type Alert = {
  id: string,
  original_message: string | null,
  affiliates_program: string | null,
  trip: string | null,
  route: string | null,
  miles: string | null,
  amount: string | null,
  type_trip: string | null,
  airlines: string | null,
  remaining: string | null,
  sent: string,
  sent_date: string | null,
  created_at: string | null,
  link: string | null
}